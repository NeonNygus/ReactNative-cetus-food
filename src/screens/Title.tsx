import { StyleSheet, Text, View } from "react-native";

const Title = () => {
  return (
    <View>
      <Text>Title</Text>
    </View>
  );
};

export default Title;

const styles = StyleSheet.create({});
